import 'package:flutter/material.dart';

class InsuranceCmpcard extends StatelessWidget {
  const InsuranceCmpcard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}